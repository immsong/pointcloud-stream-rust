[package]
name = "pointcloud-stream"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = [
    "net",
    "rt-multi-thread",
    "macros",
    "time",
    "sync",
] }
tokio-tungstenite = "0.30"
futures-util = "0.3"
